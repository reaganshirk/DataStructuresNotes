/*

LinkedList<int>* one =new LinkedList<int>();

template <class DT>
LinkedList<DT>::LinkedList()
{
	_info = NULL;
	_next = NULL;
}

cout << (*one).size(); << endl;

template <class DT>
int LinkedList<DT>::size() 
{
	if (_info == NULL)
	{
		return 0;
	}

	return (1 + (*_next).size();
}

*/